# Sample Outputs

This folder contains small, human-readable reports from local checkpoints.

The reports are safe to track in Git. They do not include checkpoints, training corpora, or generated experiment directories.

Current report:

- `sentinel_multitask_10_each_three_block.md`: 10 regex, 10 addition, and 10 subtraction generations from the local best sentinel multitask checkpoint.

